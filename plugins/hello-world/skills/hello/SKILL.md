---
description: Say hello and confirm the demo marketplace is working
---

Say hello! Confirm that:
1. The `gradion-demo` marketplace was added successfully
2. The `hello-world` plugin was installed successfully
3. This `/hello-world:hello` skill is now available

Then give a one-sentence tip about what else Claude Code plugins can do (skills, agents, hooks, MCP servers).
